import { createTheme } from '@mui/material';

export const Bootstraptheme = createTheme({
  palette: {
    primary: {
      main: '#154AB6',
    },
    error: {
      main: 'rgb(238, 83, 80)',
    },
  },
});

export const DatePickerTheme = createTheme({
  palette: {
    primary: {
      main: '#154AB6',
    },
    error: {
      main: 'rgb(238, 83, 80)',
    },
  },
});

export const StepTheme = createTheme({
  palette: {
    text: {
      primary: '#4C81EB',
      secondary: '#262E35',
    },
    error: {
      main: 'rgb(238, 83, 80)',
    },
  },
});
